# Vercel Postgres — auto-set when you add Storage → Postgres in Vercel dashboard
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# One-time DB setup: visit /api/setup?key=YOUR_SECRET after deploy
SETUP_SECRET=change-me-before-deploy
