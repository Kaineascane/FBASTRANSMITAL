# Local dev: leave POSTGRES_URL empty — app uses data/transmittal.db automatically
# Vercel: add Storage → Postgres (POSTGRES_URL is set for you)

SETUP_SECRET=fbas-local-setup-2026

# POSTGRES_URL=postgresql://user:password@host/database?sslmode=require
# POSTGRES_PRISMA_URL=
# POSTGRES_URL_NON_POOLING=
